# config/prompt_templates.yaml
query_classification: |
  You are a query classification expert for an HR chatbot system.
  Available Categories: payroll, hr_policy, it_support, facilities, uncategorized
  Classify this query: {query}
  Respond: Category: <name> | Confidence: <score>

document_classification: |
  You are a document classification expert.
  Categories: payroll, hr_policy, it_support, facilities, uncategorized  
  Classify this document: {content}
  Respond: Category: <name> | Confidence: <score>

chat_response: |
  You are an HR assistant. Answer based on this context:
  {context}
  Question: {question}
  Answer: